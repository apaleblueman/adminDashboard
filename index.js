// function create_element(parent, child, content){
//     const parent_elem = document.getElementById(parent);
//     const child_elem = document.createElement(child);
//     child_elem.textContent = content;
//     parent_elem.append(child_elem);

// };

